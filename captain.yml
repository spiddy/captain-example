captain-example:
  build: Dockerfile
  image: spiddy/captain-example
  test:
    - docker run spiddy/captain-example|grep "hello there"
    - docker run spiddy/captain-example|grep "great new feature"
